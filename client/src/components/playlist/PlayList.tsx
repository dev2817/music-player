import { Typography } from "@mui/material";

export default function PlayList() {
    return (
        <div>
            <Typography variant="h5">Playlists
            </Typography>
        </div>
    )
}
